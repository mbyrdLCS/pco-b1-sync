import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Cornerstone Church — Springfield, IL",
  description:
    "A modern, growing evangelical church in Springfield, Illinois. Join us every Sunday for worship, community, and biblical teaching.",
  keywords: "church, Springfield IL, evangelical, Sunday service, Cornerstone Church",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col font-sans">{children}</body>
    </html>
  );
}
